# {{project}} Production Server
_Last updated: {{now}}_
_infra-kit v{{infraKitVersion}}_

> This file is auto-generated by `infra-kit sync`.
> To make changes, update `.infra/config.json` then run `infra-kit sync`.

---

## Server

| Field | Value |
|---|---|
| Provider | {{server.provider}} |
| Model | {{server.model}} |
| IP | {{server.ip}} |
| SSH Port | {{server.port}} |
| SSH User | {{server.user}} |
| Location | {{server.location}} |

---

## Stack

| Layer | Technology |
|---|---|
| Orchestrator | Docker Swarm |
| PaaS | Coolify → http://localhost:8000 |
| Reverse Proxy | Traefik (managed by Coolify) |
| Runtime | Docker 26.x |

---

## Services

| Service | Config Path | Port |
|---|---|---|
{{#each services}}
| {{name}} | {{path}}/docker-compose.yml | {{dash port}} |
{{/each}}

### Directory Structure

```
/opt/{{project}}/
{{#each services}}
├── {{stripProject name ../project}}/
{{/each}}
└── scripts/
    ├── healthcheck.sh
    ├── scale.sh
    └── backup.sh
```

---

## Scaling Policy

| Parameter | Value |
|---|---|
| Scale UP trigger | CPU > {{scaling.cpuUp}}% (5 min) |
| Scale DOWN trigger | CPU < {{scaling.cpuDown}}% (10 min) |
| Min replicas | {{scaling.minReplicas}} |
| Max replicas | {{scaling.maxReplicas}} |

**Decision table:**

| Condition | Autonomous | Ask User |
|---|---|---|
| Container crashed | ✅ restart | — |
| Disk > 80% | ✅ docker prune | — |
| Worker stalled | ✅ restart | — |
| Scale UP/DOWN | — | ✅ |
| DB migration | — | ✅ |
| Config change | — | ✅ |

---

## Backup

| Field | Value |
|---|---|
| Schedule | {{backup.schedule}} (UTC) |
| Local retention | {{backup.retentionDays}} days |
| S3 bucket | {{backup.s3Bucket}} |
| Path | /opt/{{project}}/db/backups/ |

---

## Agent Rules

1. **Never** log or print `.env` contents
2. **Never** `rm -rf /opt/{{project}}/db/`
3. Do not use `docker compose down` in production directly
4. No replica reduction without approval
5. Run `infra-kit sync` after every operation

---

## Notes

_This section is updated manually._
